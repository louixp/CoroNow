import string_arr_to_XML

#all tweets within an entire timestamp directory on FireBase (like 2020-03-26-13) => single xml file 
def save_xml(firebase, time):
    end_time[-1] = end_time[-1] + 30
    keywords = []
    for 


    firebase.retrieve_data("tweets", , f)

    xml = convert_to_XML(firebase, start_time=[], end_time=end_time, keywords=keywords, entry="tweet"):


